// const dotenv = require("dotenv");
// const auth0utils = require("../utils/auth0utils");

// dotenv.config();

// module.exports.config = async () => {
//   const token = await auth0utils.getToken();
//   process.env.API_TOKEN = token;
// };
